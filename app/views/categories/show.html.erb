<div class="container">
  <h1>Show Page</h1>
  <h1><%= @category.name %></h1>
  <%= link_to "Edit", edit_category_path(@category) %>
  <%= link_to "Delete", edit_category_path(@category),
                                      method: :delete,
                                      data: { confirm: "Are you Sure?"} %>
</div>
